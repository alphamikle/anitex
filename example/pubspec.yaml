name: example
description: Anitex example project
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # >1.0.0: Dart >=2.12.0-0 <3.0.0
  cupertino_icons: ^1.0.0

  anitex:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
